@model IEnumerable<BookStore.Models.Book>

@{
    ViewData["Title"] = "Store";
}

<h2 class="my-2">Store</h2>


@* <form asp-action="Index" class="col-2 my-1 p-2">
    <div>
        Filter search
        <div class="my-1">
            <input type="text" name="searchString" placeholder="Title or Author" class="form-control-sm col-12" />
        </div>
        <div class="my-1">
            <input type="number" name="minPrice" step="1" placeholder="$min" class="form-control-sm col-5" />&nbsp;&#8212;&nbsp;<input type="number" name="maxPrice" step="1" placeholder="$max" class="form-control-sm col-5" />
        </div>
        <input type="submit" value="Filter" class="btn btn-primary btn-sm" />
        <a asp-action="Index" class="btn btn-secondary btn-sm float-right">Clear</a>
    </div>
</form> *@

<style>
    /* Áp dụng đường viền cho cả input text và input number */
    input[type="text"],
    input[type="number"] {
        border: 1px solid #ced4da;
        border-radius: 0.25rem;
    }
</style>

<div class="row justify-content-center">
    <form asp-action="Index" class="col-md-6 my-1 p-2">
        <div class="text-center">
            @* Filter search *@
            <div class="my-1 input-group">
                <div class="input-group-prepend">
                    <span class="input-group-text"><i class="fa-solid fa-magnifying-glass"></i></span>
                </div>
                <input type="text" name="searchString" step="1" placeholder="Title or Author" class="form-control form-control-sm col-12" />
            </div>
            <div class="my-1 mt-2">
                <input type="number" name="minPrice" step="1" placeholder="$min" class="form-control-sm col-5" />&nbsp;&#8212;&nbsp;<input type="number" name="maxPrice" step="1" placeholder="$max" class="form-control-sm col-5" />
            </div>
            <div class="my-1 mt-2">
                <input type="submit" value="Filter" class="btn btn-primary btn-sm" />
                <a asp-action="Index" class="btn btn-secondary btn-sm ml-2">Clear</a>
            </div>
        </div>
    </form>
</div>


<div class="row">
    @foreach (var book in Model)
    {
        <div class="col-3">
            <div class="card my-2 store-card">
                <a class="a-cleanup" asp-controller="Store" asp-action="Details" asp-route-id="@book.Id">
                    <img src="@book.ImageUrl" class="card-img-top card-img-custom">
                    <div class="card-body" style="height: 6em;">
                        <h5 class="mb-3">@book.Title</h5>
                    </div>
                    <div class="card-body">
                        <span class="card-text">@book.Price.ToString("c")</span>
                        <a asp-controller="Cart" asp-action="AddToCart" asp-route-id="@book.Id" class="btn btn-primary float-right store-btn"><i class="fa-solid fa-cart-plus"></i></a>
                    </div>
                </a>
            </div>
        </div>
    }
</div>